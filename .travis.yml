language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-jekyllstarter' ]; then cd .. && eval "mv $currentfolder generator-jekyllstarter" && cd generator-jekyllstarter; fi

